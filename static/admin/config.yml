backend:
  name: git-gateway
  branch: master

media_folder: static/images/uploads
public_folder: /images/uploads

slug:
  encoding: 'ascii'
  clean_accents: true

collections:
  - name: 'post'
    label: 'Post'
    folder: 'posts'
    create: true
    slug: '{{fields.slug}}'
    path: '{{slug}}/index'
    editor:
      preview: false
    fields:
      - name: 'title'
        label: 'Title'
        widget: 'string'
        required: true
      - name: 'datetime'
        label: 'Publish Date'
        widget: 'datetime'
        required: true
      - name: 'slug'
        label: 'Path'
        widget: 'string'
        required: true
      - name: 'poster'
        label: 'Poster'
        widget: 'object'
        fields:
          - name: 'local'
            label: 'Upload'
            widget: 'image'
            required: false
          - name: 'external'
            label: 'URL'
            widget: 'string'
            required: false
          - name: 'alt'
            label: 'Alt'
            widget: 'string'
            required: false
          - name: 'title'
            label: 'Title'
            widget: 'string'
            required: false
      - name: 'body'
        label: 'Body'
        widget: 'markdown'
        required: true
